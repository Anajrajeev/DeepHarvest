# Prometheus Configuration for DeepHarvest
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'deepharvest'
    static_configs:
      - targets:
          - 'coordinator:9090'
          - 'worker1:9090'
          - 'worker2:9090'
    metrics_path: '/metrics'

